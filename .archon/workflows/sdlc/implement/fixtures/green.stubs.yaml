# Dry-run fixture: the green path. Fully stubbed — proves the DAG wiring,
# input binding, and until_field completion without executing anything.
# Structured stubs are YAML mappings (they hydrate structuredOutput, which the
# until_field check reads); plain-string stubs stay strings.
implement:
  done: true
  green: true
  red_cause: ""
  summary: "stub: work complete, checks green"
assert-changed: "1 file changed"
# The repository this branch's pull requests belong to. Stubbed because the
# node is deterministic and resolves from git's push configuration, which a
# fixture checkout does not have.
pr-target:
  repo: "example/repo"
  reason: ""
fixture:
  expect: completed
